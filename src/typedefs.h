#ifndef _TYPEDEFS_H
#define _TYPEDEFS_H

#include <iostream>
#include <vector>
#include <map>

typedef map < string,float > t_percents;
typedef map < string,t_percents > t_results;

#endif /* _TYPEDEFS_H */

